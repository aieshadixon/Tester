(parameters) => expression
// are the input values or arguments that the function takes

const functionName = (parameters) => expression

const add = (a, b) => a + b;

function add(a, b) {
    return a + b;
}